import React from 'react';
import { withStyles } from '@material-ui/core/styles';

import styles from './calculator-results-styles';

const CalculatorResults = (props) => {
  console.log(props);
  return (
    <div>hello</div>
  );
};

export default withStyles(styles)(CalculatorResults);
