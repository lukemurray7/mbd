const styles = theme => ({
  buttons: {
    marginLeft: 'auto',
  },
  main: {
    zIndex: theme.zIndex.drawer + 1,
    height: 50,
    boxShadow: 'none',
  },
  toolbar: {
    minHeight: 50,
  },
});

export default styles;
